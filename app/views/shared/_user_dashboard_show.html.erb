<h1>Welcome <%= current_user.name %>!</h1>

<p>Delete my account:</p>

<%= form_tag "/users/destroy/#{current_user.id}", method: :delete do %>
  <%= submit_tag "Delete", class: :button %>
<% end %>

<%= render "shared/npc_list" %>
